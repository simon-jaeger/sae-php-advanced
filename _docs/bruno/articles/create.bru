meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/articles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "laravel",
    "content": "a php framework"
  }
}
