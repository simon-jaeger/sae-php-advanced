info:
  name: conversation
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/agents/conversation"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "msg": "Is this my first message?"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
