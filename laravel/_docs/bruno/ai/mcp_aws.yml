info:
  name: mcp_aws
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/ai/mcp"
  body:
    type: json
    data: |-
      {
        "url": "https://knowledge-mcp.global.api.aws",
        "tool": "aws___search_documentation",
        "args": {
          "search_phrase": "s3 javascript sdk"
        }
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
