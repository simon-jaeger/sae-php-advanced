info:
  name: nsfw
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/ai/nsfw"
  body:
    type: json
    data: |-
      {
        "id": 23
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
