info:
  name: summarize
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/ai/summarize"
  body:
    type: json
    data: |-
      {
        "id": 21
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
