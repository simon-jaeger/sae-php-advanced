info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/articles"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "title": "fuck this shit",
        "content": "let's talk about hard drugs and crime"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
