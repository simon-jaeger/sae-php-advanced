info:
  name: destroy
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/articles"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
