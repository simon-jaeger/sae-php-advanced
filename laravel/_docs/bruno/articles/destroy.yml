info:
  name: destroy
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/articles"
  body:
    type: json
    data: |-
      {
        "id": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
