info:
  name: update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/articles"
  body:
    type: json
    data: |-
      {
        "id": 4,
        "title": "my new title",
        "content": "..."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
