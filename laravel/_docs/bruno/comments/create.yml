info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/comments"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "text": "awesome!",
        "article_id": 1
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
