info:
  name: destroy
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/comments"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "id": 1
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
