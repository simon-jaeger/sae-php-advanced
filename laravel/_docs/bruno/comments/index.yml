info:
  name: index
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/comments"
  headers:
    - name: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
