info:
  name: update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/comments"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "id": 1,
        "text": "changed comment"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
