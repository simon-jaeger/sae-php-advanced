info:
  name: echo
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/examples/echo"
  body:
    type: json
    data: |-
      {
        "text": "asdf"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
