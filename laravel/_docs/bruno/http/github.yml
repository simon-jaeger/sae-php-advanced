info:
  name: github
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/http/github?user=simon-jaeger"
  params:
    - name: user
      value: simon-jaeger
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
