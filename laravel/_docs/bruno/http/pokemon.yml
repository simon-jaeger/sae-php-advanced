info:
  name: pokemon
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/http/pokemon"
  params:
    - name: ids
      value: 1,2,3
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "ids": [1,4,7,25]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
