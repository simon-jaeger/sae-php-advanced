info:
  name: newsletter
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/mails/newsletter"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "user_id": 3
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
