info:
  name: palindrom
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/examples/palindrom"
  body:
    type: json
    data: |-
      {
        "text": "anna"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
