info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/tags"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "name": "dolor"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
