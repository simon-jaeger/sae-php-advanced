info:
  name: destroy
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/uploads"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "id": 1
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
