info:
  name: show
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/uploads/:id"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  params:
    - name: id
      value: "2"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
