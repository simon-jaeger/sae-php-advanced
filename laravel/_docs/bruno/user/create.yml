info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/user"
  body:
    type: json
    data: |-
      {
        "email": "alpha@mailinator.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
