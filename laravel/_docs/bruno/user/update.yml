info:
  name: update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/user"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "name": "alpha2"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
